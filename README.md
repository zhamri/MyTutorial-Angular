## Get API (jsonplaceholder)
```aiignore
https://jsonplaceholder.typicode.com/users
```

## Run the Application
```aiignore
http://localhost:4200/
```

## Step-by-step debug in browser


## Note:
1. In Angular 17+ (including Angular 20), the traditional structural directives like *ngIf, *ngFor, and *ngSwitch are not deprecated, but Angular introduced a new syntax called the “@if / @for / @switch” block syntax as the modern replacement.
2. So *ngIf still works (for backward compatibility), but the recommended syntax in Angular 17+ is to use @if and @for.
